Used create-react-app as boiler plate.

The three tools I used include:
react-dropzone to accept an image from a user in the dropbox.
superagent to transfer the uploaded image
Cloudinary to store and manipulate the images

Setup cloudinary account and setup preset
Get the api key and the api url
